[pytest]
testpaths = tests
markers =
    local_only: needs the MLflow production model artifact (predict/explain/API output); runs locally and is reviewer-gated, skipped in CI with -m "not local_only"
